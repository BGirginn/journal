#!/usr/bin/env bash

set -euo pipefail

# FVM-first flutter wrapper.
if command -v fvm >/dev/null 2>&1; then
  exec fvm flutter "$@"
fi

if command -v flutter >/dev/null 2>&1; then
  exec flutter "$@"
fi

echo "flutterw: neither 'fvm' nor 'flutter' command found." >&2
exit 1
